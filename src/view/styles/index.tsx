import {css} from "@emotion/react";
import {theme} from "@view/styles/theme";

export const iconBaseStyle = css`
  width: 30px;
  height: 30px;
`;

export const iconSmallStyle = css`

    color: white;
    display: inline-block;
    //box-shadow: 1px 1px 2px 0 rgba(0, 0, 0, 0.2);
  `;